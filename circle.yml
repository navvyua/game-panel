dependencies:
  pre:
    - cp config/secrets.yml.example config/secrets.yml
  post:
    - bundle exec rubocop
    - bundle exec jshint
deployment:
  staging:
    branch: master
    heroku:
      appname: game-panel
